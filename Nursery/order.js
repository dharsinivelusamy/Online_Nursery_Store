function getText(){
    var text = document.getElementById("plants").textContent;
    document.getElementById("plants").innerText = text;
    }
function getPlant()
{

const areaSelect = document.querySelector(`[id="plants"]`);

areaSelect.addEventListener(`click`, (e) => {
  // log(`e.target`, e.target);
  const select = e.target;
}
}